{
  "name": "rbes-playbook",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npx @11ty/eleventy",
    "deploy": "npm run build "deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push","deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push", echo rulebreakers.studio > docs/CNAME "deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push","deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push", git add docs "deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push","deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push", git commit -m 'Deploy site' || echo No changes "deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push","deploy": "npm run build && echo rulebreakers.studio > docs/CNAME && git add docs && git commit -m 'Deploy site' || echo "No changes" && git push", git push",
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "dependencies": {
    "luxon": "^3.7.2"
  }
}
